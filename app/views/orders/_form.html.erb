<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
        <% order.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :userId %>
    <%= form.number_field :userId %>
  </div>

  <div class="field">
    <%= form.label :orderItemId %>
    <%= form.number_field :orderItemId %>
  </div>

  <div class="field">
    <%= form.label :transactionId %>
    <%= form.text_field :transactionId %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :unitPrice %>
    <%= form.text_field :unitPrice %>
  </div>

  <div class="field">
    <%= form.label :orderPrice %>
    <%= form.text_field :orderPrice %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
